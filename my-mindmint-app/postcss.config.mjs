const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

// NOTE: This config must be used ONLY from /mindmint/my-mindmint-app directory.
// Running the build from the parent /mindmint directory will cause resolution errors.

export default config;
